# Dokploy module secrets
# Copy this file to .env and fill in the values

# PostgreSQL password for Dokploy database (generate a strong random password)
DOKPLOY_POSTGRES_PASSWORD=your-secure-password-here

# Private IP address for Dokploy ADVERTISE_ADDR
# Get your private IP with: ip addr show | grep -E "inet (192\.168\.|10\.|172\.1[6-9]\.|172\.2[0-9]\.|172\.3[0-1]\.)" | head -n1 | awk '{print $2}' | cut -d/ -f1
DOKPLOY_ADVERTISE_ADDR=192.168.1.100
